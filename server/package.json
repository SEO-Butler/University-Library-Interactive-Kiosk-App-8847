{
  "name": "library-kiosk-server",
  "private": true,
  "version": "2.0.0",
  "description": "API, CMS backend and static host for the library kiosk (PostgreSQL + S3-compatible object storage)",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "migrate": "node cli.js migrate",
    "seed": "node cli.js seed",
    "user": "node cli.js",
    "test": "node --test"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "cookie-parser": "^1.4.7",
    "express": "^5.1.0",
    "multer": "^2.0.2",
    "pg": "^8.13.0"
  }
}
